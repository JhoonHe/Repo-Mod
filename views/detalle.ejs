<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/public/css/style-detalle.css">
</head>
<body>
    <main>
        <header class="header">
            <div class="logo"><a href="/">LOGO</a></div>
            <input type="search">
            <nav>
                <ul class="nav-links">
                    <li><a href="/">Home</a></li>
                    <li>
                        <%if (nombres) { %>
                            <a href="#"><%= nombres %></a>
                        <% } %>
                    </li>
                    <li>
                        <%if (!nombres) { %> 
                            <a href="/registro-interfaz">Register</a>
                        <% } %>
                    </li>
                    <li>
                        <%if (!nombres) { %> 
                            <a href="/login-interfaz">Login</a>
                            <% } %>
                    </li>
                    <li>
                        <% if(nombres) { %>
                            <a href="/logout">Exit</a> 
                        <% } %> 
                    </li>
                </ul>
            </nav>
        </header>
        <div class="detalle">
            <div class="img-articulo">
                <img src="<%= articulos[0].url %>" alt="imagen-articulo">
            </div>
            <div class="info-articulo">
                <h2><%= articulos[0].nombre %></h2>
                <p>Valor: <%= articulos[0].valor %></p>
                <p>Proveedor: <a href="#"><%= articulos[0].proveedor %></a></p>
            </div>
            <form action="/comprar/<%= articulos[0].codigo %>" method="post">
            <button class="comprar">Comprar</button>
            </form>
        </div>
            <div class="descripcion">
                <p><%= articulos[0].descripcion%></p>
            </div>
        <div class="articulos"></div>
    </main>
</body>
</html>